// 6/23/15

// review hangman
// review of jQuery

// for jquery, instead of window.function(), do this
$(function() {

})

// cdnjs - search engine for code libraries. look it up on google

//useful jQuery stuff

$('<tag>'); 
$('tag');
$('#id');
$('.class');

.attr('class', 'name of class');
.addClass('name of another class');

.attr('id', "name of id");
$('<tag id="id name">')

// check out this example
body.append("<h1>").text("hi");
// this will create h1 tag, append it to the body, and then will create a text "hi" to BODY!

//grab the pony with id and set the interval of it 
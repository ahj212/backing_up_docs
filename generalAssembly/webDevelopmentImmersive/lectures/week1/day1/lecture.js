// 6/8/15

// week 1: javascript fundamentals

// variable contains values; could be numbers strings, booleans
// think of variables as a way for javascript to grasp values

// computer: based on the turing machine (Allan Turing)
// programming abstraction
// ENIAC: first vacuum tube computer

// variable is a value that we assign on a computer

// MONDAY
// students will be able to
// - use primative datatype
// - use control flow structure
// use typcasting appropriately
// use breakpoints to debug

// primative datatypes are string, number, booleans
// eg. "hi", "bye", 3, true

// basic string methods
// .toUpperCase();
// .toLowerCase();

// javascript doesn't work well with decimals
// for example, is you put 1.3 - 1.2 in javascript, it will not output .1

// another primative datatype is boolean
// 0, undefined, null, and "" are all considered falsey

// conditionals are any statement that can be true or false
// && is "and" operator, || is "or" operater

if (conditional) {
	something happens;
}

// BREAK

// scaffolding html document
// after typing html, press tab
// after typing script, press tab

// ! is pronounced bang. means "not"
// parseInt("3") returns number 3: parseInt changes a string into an integer

// \ backslash turns quotes into a character
// e.g. 'sally\'s' will return sally's

// breakpoint -- debugger; stops a line
// google chrome debugger

// we "capture" something with a variable. 
